const EndGame = () => {
  return (
    <div className="restart">
      <button
        className="restart-btn"
        // onClick={startGame}
        // data-testid="newGameButton"
      >
        PLAY AGAIN
      </button>
    </div>
  );
};

export default EndGame;
